version: '3.8'

services:
  db:
    image: postgres:15
    container_name: theater-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: bancodeteste
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: